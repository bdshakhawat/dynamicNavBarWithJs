# To create a nav bar dynamically we need to declare a variable of array object named menuList.
# Now write a function named mainFunction. Inside the function create a div with document.createElement("div")
# After that put some html tags like ul inside the div.
# append the div  to the body with appendChild 
# Write another function navlist.
# Inside this function run map function on menuList to get values of menuName.
# create a li with document.createElement(li) and insert the menuName valus in li
# Now append the li inside ul with appendChild(list_Item)
# Finally call the navList and main function.




